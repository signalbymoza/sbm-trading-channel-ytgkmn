
# 🔔 إعداد Webhook لـ Stripe - دليل سريع

## ⚠️ هام جداً: يجب إعداد Webhook

بدون Webhook، لن يعرف التطبيق متى تنجح المدفوعات. سيدفع المستخدمون لكن لن يحصلوا على الوصول إلى القنوات!

---

## 📋 خطوات الإعداد

### الخطوة 1: افتح لوحة تحكم Stripe
اذهب إلى: https://dashboard.stripe.com/webhooks

### الخطوة 2: أضف نقطة نهاية جديدة
1. انقر على "Add endpoint" (إضافة نقطة نهاية)
2. أدخل عنوان URL:
   ```
   https://hwynfzyvj4kkcgzps5adtetqrpeqfjnj.app.specular.dev/api/stripe/webhook
   ```

### الخطوة 3: اختر الأحداث
حدد هذه الأحداث:
- ✅ `payment_intent.succeeded` (نجح الدفع)
- ✅ `payment_intent.payment_failed` (فشل الدفع)
- ✅ `checkout.session.completed` (اكتملت جلسة الدفع)

### الخطوة 4: احفظ واحصل على المفتاح السري
1. انقر على "Add endpoint" (إضافة)
2. انسخ **webhook signing secret** (يبدأ بـ `whsec_...`)
3. أضفه إلى متغيرات البيئة في الخادم:
   ```
   STRIPE_WEBHOOK_SECRET=whsec_...
   ```

---

## ✅ التحقق من عمل Webhook

### كيف تتحقق؟
1. قم بإجراء دفعة تجريبية
2. اذهب إلى: https://dashboard.stripe.com/webhooks
3. انقر على نقطة النهاية التي أنشأتها
4. تحقق من علامة التبويب "Events" (الأحداث)
5. يجب أن ترى الأحداث مع حالة "Succeeded" (نجح)

### إذا لم يعمل Webhook:
- تحقق من أن عنوان URL صحيح
- تأكد من أن المفتاح السري مُعد في الخادم
- تحقق من أن الخادم يمكن الوصول إليه (غير محظور)

---

## 🎯 ماذا يفعل Webhook؟

عندما يدفع المستخدم:
1. Stripe يعالج الدفع
2. Stripe يرسل حدث إلى Webhook الخاص بك
3. الخادم يستقبل الحدث
4. الخادم يفعّل الاشتراك
5. المستخدم يحصل على الوصول إلى القناة

---

## 📧 الإشعارات التلقائية

بعد إعداد Webhook، سيتم إرسال:
- ✅ بريد تأكيد للمستخدم
- ✅ رابط قناة Telegram
- ✅ إشعار للمسؤول
- ✅ تفعيل الاشتراك تلقائياً

---

## ⚠️ تذكير مهم

**بدون Webhook:**
- ❌ لن يتم تفعيل الاشتراكات
- ❌ لن يتم إرسال رسائل التأكيد
- ❌ لن يحصل المستخدمون على الوصول

**مع Webhook:**
- ✅ تفعيل تلقائي للاشتراكات
- ✅ إرسال رسائل تأكيد فوري
- ✅ وصول فوري للمستخدمين

---

## 🆘 هل تحتاج مساعدة؟

إذا واجهت مشاكل:
1. تحقق من سجلات Webhook في لوحة التحكم
2. راجع سجلات الخادم
3. تواصل مع دعم Stripe: https://support.stripe.com

---

**إعداد Webhook ضروري قبل قبول المدفوعات الحقيقية!**

*آخر تحديث: يناير 2025*
